<Page x:Class="CORCleanup.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:CORCleanup.ViewModels"
      d:DataContext="{d:DesignInstance vm:HomeViewModel}"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Dashboard"
      Background="{DynamicResource ApplicationBackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" CanContentScroll="False">
        <StackPanel Margin="12,8">

            <!-- ============================================================ -->
            <!-- Header: Title + Refresh + Status                             -->
            <!-- ============================================================ -->
            <DockPanel Margin="0,0,0,10">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Dashboard" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock Text="  Loading..." FontSize="11" Opacity="0.4"
                               VerticalAlignment="Center"
                               Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVis}}" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <TextBlock Text="{Binding ViewModel.StatusText}" FontSize="10" Opacity="0.4"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ui:Button Appearance="Secondary"
                               Command="{Binding ViewModel.RefreshDashboardCommand}"
                               ToolTip="Refresh dashboard"
                               Padding="4,3">
                        <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="14" />
                    </ui:Button>
                </StackPanel>
            </DockPanel>

            <!-- ============================================================ -->
            <!-- Row 0: System Identity — 4 tiles                             -->
            <!--   PC Name | CPU | Network | GPU                              -->
            <!-- ============================================================ -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- PC Name + OS -->
                <Border Grid.Column="0" Margin="0,0,4,0" Padding="12,8"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <DockPanel>
                        <Border DockPanel.Dock="Left"
                                Width="32" Height="32" CornerRadius="8"
                                Margin="0,0,8,0"
                                Background="#1A06B6D4">
                            <ui:SymbolIcon Symbol="Desktop24" FontSize="14"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Foreground="#06B6D4" />
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding ViewModel.ComputerName, FallbackValue='Loading...'}"
                                       FontSize="12" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding ViewModel.OsCaption, FallbackValue='Windows'}"
                                       FontSize="10" Opacity="0.5"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- CPU -->
                <Border Grid.Column="1" Margin="4,0,4,0" Padding="12,8"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <DockPanel>
                        <Border DockPanel.Dock="Left"
                                Width="32" Height="32" CornerRadius="8"
                                Margin="0,0,8,0"
                                Background="#1A8B5CF6">
                            <ui:SymbolIcon Symbol="TopSpeed24" FontSize="14"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Foreground="#8B5CF6" />
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding ViewModel.CpuShortName, FallbackValue='CPU'}"
                                       FontSize="12" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding ViewModel.CpuSummary, FallbackValue='...'}"
                                       FontSize="10" Opacity="0.5"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- Network (moved from middle section to top bar) -->
                <Border Grid.Column="2" Margin="4,0,4,0" Padding="12,8"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <DockPanel>
                        <Border DockPanel.Dock="Left"
                                Width="32" Height="32" CornerRadius="8"
                                Margin="0,0,8,0"
                                Background="#1A06B6D4">
                            <ui:SymbolIcon Symbol="Globe24" FontSize="14"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Foreground="#06B6D4" />
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding ViewModel.LocalIpAddress, FallbackValue='Network'}"
                                       FontSize="12" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding ViewModel.WanIpAddress, FallbackValue='...'}"
                                       FontSize="10" Opacity="0.5"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- GPU -->
                <Border Grid.Column="3" Margin="4,0,0,0" Padding="12,8"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <DockPanel>
                        <Border DockPanel.Dock="Left"
                                Width="32" Height="32" CornerRadius="8"
                                Margin="0,0,8,0"
                                Background="#1AF59E0B">
                            <ui:SymbolIcon Symbol="Video24" FontSize="14"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Foreground="#F59E0B" />
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding ViewModel.GpuShortName, FallbackValue='GPU'}"
                                       FontSize="12" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding ViewModel.GpuVram, FallbackValue='...'}"
                                       FontSize="10" Opacity="0.5" />
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- ============================================================ -->
            <!-- Row 1: Two-column layout                                     -->
            <!--   LEFT  (~50%): RAM Info + Memory Gauge                      -->
            <!--   RIGHT (~50%): Storage + Disk Health                        -->
            <!-- ============================================================ -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- ======== LEFT COLUMN: RAM Info + Memory Gauge ======== -->
                <StackPanel Grid.Column="0" Margin="0,0,4,0">

                    <!-- RAM Info (slots, type, speed) -->
                    <Border Padding="12,10" CornerRadius="6" Margin="0,0,0,6"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <DockPanel>
                            <Border DockPanel.Dock="Left"
                                    Width="32" Height="32" CornerRadius="8"
                                    Margin="0,0,8,0"
                                    Background="#1A10B981">
                                <ui:SymbolIcon Symbol="SlideSize24" FontSize="14"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Foreground="#10B981" />
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding ViewModel.RamDisplay, FallbackValue='RAM'}"
                                           FontSize="12" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding ViewModel.RamSlots, FallbackValue='...'}"
                                           FontSize="10" Opacity="0.5" />
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <!-- Memory Gauge (usage bar) -->
                    <Border Padding="12,10" CornerRadius="6"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,6">
                                <TextBlock Text="MEMORY USAGE" FontSize="9" FontWeight="SemiBold"
                                           Opacity="0.35" DockPanel.Dock="Left"
                                           VerticalAlignment="Center" />
                                <TextBlock DockPanel.Dock="Right" HorizontalAlignment="Right"
                                           FontSize="10.5" VerticalAlignment="Center">
                                    <Run Text="{Binding ViewModel.MemoryUsed, Mode=OneWay}" FontWeight="SemiBold" />
                                    <Run Text=" / " Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                    <Run Text="{Binding ViewModel.MemoryTotal, Mode=OneWay}" />
                                    <Run Text=" (" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                    <Run Text="{Binding ViewModel.MemoryPercent, Mode=OneWay, StringFormat={}{0}}" FontWeight="SemiBold" />
                                    <Run Text="%)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                </TextBlock>
                            </DockPanel>
                            <ProgressBar Value="{Binding ViewModel.MemoryPercent, Mode=OneWay}"
                                         Maximum="100" Height="14" />
                            <TextBlock Margin="0,4,0,0" FontSize="10" Opacity="0.4">
                                <Run Text="Health: " />
                                <Run Text="{Binding ViewModel.MemoryHealth, Mode=OneWay}" />
                            </TextBlock>
                        </StackPanel>
                    </Border>

                </StackPanel>

                <!-- ======== RIGHT COLUMN: Storage + Disk Health ======== -->
                <StackPanel Grid.Column="1" Margin="4,0,0,0">

                    <!-- Storage section -->
                    <Border Padding="12,10" CornerRadius="6" Margin="0,0,0,6"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <StackPanel>
                            <TextBlock Text="STORAGE" FontSize="9" FontWeight="SemiBold"
                                       Opacity="0.35" Margin="0,0,0,6" />
                            <ItemsControl ItemsSource="{Binding ViewModel.LogicalDrives}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="11.5"
                                                       FontWeight="SemiBold" VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis" />

                                            <ProgressBar Grid.Column="1"
                                                         Value="{Binding UsedPercent, Mode=OneWay}"
                                                         Maximum="100" Height="12"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,10,0" />

                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock FontSize="10" Opacity="0.7">
                                                    <Run Text="{Binding UsedDisplay, Mode=OneWay}" />
                                                    <Run Text=" / " />
                                                    <Run Text="{Binding TotalDisplay, Mode=OneWay}" />
                                                </TextBlock>
                                                <TextBlock Text="{Binding FreeDisplay}" FontSize="9.5" Opacity="0.4"
                                                           Margin="6,0,0,0" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Disk Health section -->
                    <Border Padding="12,10" CornerRadius="6"
                            Background="{DynamicResource ControlFillColorDefaultBrush}">
                        <StackPanel>
                            <TextBlock Text="DISK HEALTH" FontSize="9" FontWeight="SemiBold"
                                       Opacity="0.35" Margin="0,0,0,6" />
                            <ItemsControl ItemsSource="{Binding ViewModel.PhysicalDisks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,5" Padding="10,7"
                                                CornerRadius="5"
                                                Background="{DynamicResource ApplicationBackgroundBrush}">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Model}" FontSize="11" FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis" />
                                                <WrapPanel Margin="0,4,0,0">
                                                    <!-- Bus + Media Type badge -->
                                                    <Border Padding="5,1" CornerRadius="3" Margin="0,0,5,0"
                                                            Background="#1A8B5CF6">
                                                        <TextBlock Text="{Binding TypeSummary}" FontSize="9.5"
                                                                   Foreground="#8B5CF6" />
                                                    </Border>
                                                    <!-- Health badge -->
                                                    <Border Padding="5,1" CornerRadius="3" Margin="0,0,5,0"
                                                            Background="#1A10B981">
                                                        <TextBlock Text="{Binding HealthDisplay}" FontSize="9.5"
                                                                   Foreground="#10B981" />
                                                    </Border>
                                                    <!-- Size -->
                                                    <TextBlock Text="{Binding SizeFormatted}" FontSize="9.5"
                                                               Opacity="0.5" VerticalAlignment="Center"
                                                               Margin="0,0,5,0" />
                                                    <!-- Temperature -->
                                                    <TextBlock Text="{Binding TemperatureDisplay}" FontSize="9.5"
                                                               Opacity="0.5" VerticalAlignment="Center" />
                                                </WrapPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Grid>

            <!-- ============================================================ -->
            <!-- Row 2: Top 5 CPU + Top 5 Memory — side by side              -->
            <!-- ============================================================ -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Top 5 CPU Processes -->
                <Border Grid.Column="0" Margin="0,0,4,0" Padding="12,10"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="TOP 5 — CPU USAGE" FontSize="9" FontWeight="SemiBold"
                                   Opacity="0.35" Margin="0,0,0,6" />
                        <ItemsControl ItemsSource="{Binding ViewModel.TopCpuProcesses}"
                                      Grid.IsSharedSizeScope="True">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" SharedSizeGroup="ProcName" />
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="ProcCpu" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}"
                                                   FontSize="11.5" TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" FontSize="11.5"
                                                   HorizontalAlignment="Right" Margin="8,0,0,0"
                                                   VerticalAlignment="Center">
                                            <Run Text="{Binding CpuFormatted, Mode=OneWay}" FontWeight="SemiBold" />
                                            <Run Text="%" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Top 5 Memory Hoggers -->
                <Border Grid.Column="1" Margin="4,0,0,0" Padding="12,10"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="TOP 5 — MEMORY USAGE" FontSize="9" FontWeight="SemiBold"
                                   Opacity="0.35" Margin="0,0,0,6" />
                        <ItemsControl ItemsSource="{Binding ViewModel.TopMemoryProcesses}"
                                      Grid.IsSharedSizeScope="True">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" SharedSizeGroup="MemName" />
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="MemVal" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}"
                                                   FontSize="11.5" TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" Text="{Binding WorkingSetFormatted}"
                                                   FontSize="11.5" FontWeight="SemiBold"
                                                   HorizontalAlignment="Right" Margin="8,0,0,0"
                                                   VerticalAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ============================================================ -->
            <!-- Row 3: Recent Errors + Outdated Drivers — side by side       -->
            <!--   Drivers list constrained with MaxHeight + scroll           -->
            <!-- ============================================================ -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Latest 5 Errors -->
                <Border Grid.Column="0" Margin="0,0,4,0" Padding="12,10"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="RECENT ERRORS (7 DAYS)" FontSize="9" FontWeight="SemiBold"
                                   Opacity="0.35" Margin="0,0,0,6" />
                        <ItemsControl ItemsSource="{Binding ViewModel.RecentErrors}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Grid.Column="0" Width="6" Height="6"
                                                 Fill="#EF4444" Margin="0,0,6,0"
                                                 VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" FontSize="10.5"
                                                   TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center">
                                            <Run Text="{Binding Source, Mode=OneWay}" FontWeight="SemiBold" />
                                            <Run Text=" " />
                                            <Run Text="{Binding EventId, Mode=OneWay}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        </TextBlock>
                                        <TextBlock Grid.Column="2" Text="{Binding TimeFormatted}"
                                                   FontSize="9.5" Opacity="0.4" Margin="6,0,0,0"
                                                   VerticalAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!-- Empty state -->
                        <TextBlock Text="No errors in the past 7 days"
                                   FontSize="10.5" Opacity="0.3" FontStyle="Italic">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ViewModel.RecentErrors.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Outdated Drivers — scrollable with MaxHeight -->
                <Border Grid.Column="1" Margin="4,0,0,0" Padding="12,10"
                        CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="OUTDATED DRIVERS (&gt;3 YEARS)" FontSize="9" FontWeight="SemiBold"
                                   Opacity="0.35" Margin="0,0,0,6" />
                        <ScrollViewer MaxHeight="160" VerticalScrollBarVisibility="Auto"
                                      CanContentScroll="False">
                            <ItemsControl ItemsSource="{Binding ViewModel.OutdatedDrivers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Ellipse Grid.Column="0" Width="6" Height="6"
                                                     Fill="#F59E0B" Margin="0,0,6,0"
                                                     VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="1" Text="{Binding DeviceName}"
                                                       FontSize="10.5" TextTrimming="CharacterEllipsis"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2" Text="{Binding AgeSummary}"
                                                       FontSize="9.5" Opacity="0.5"
                                                       Margin="8,0,0,0" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                        <!-- Empty state -->
                        <TextBlock Text="All drivers are up to date"
                                   FontSize="10.5" Opacity="0.3" FontStyle="Italic">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ViewModel.OutdatedDrivers.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ============================================================ -->
            <!-- Row 4: Quick Actions — feature shortcuts                     -->
            <!-- ============================================================ -->
            <TextBlock Text="QUICK ACTIONS" FontSize="9" FontWeight="SemiBold"
                       Opacity="0.35" Margin="0,4,0,4" />
            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:CardAction Grid.Column="0" Margin="0,0,4,0"
                               Command="{Binding ViewModel.NavigateCommand}"
                               CommandParameter="Cleanup">
                    <DockPanel Margin="4,2">
                        <ui:SymbolIcon Symbol="Broom24" DockPanel.Dock="Left"
                                      FontSize="16" Margin="0,0,6,0"
                                      Foreground="#06B6D4" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Cleanup" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="Temp files" FontSize="9.5" Opacity="0.4" />
                        </StackPanel>
                    </DockPanel>
                </ui:CardAction>

                <ui:CardAction Grid.Column="1" Margin="4,0,4,0"
                               Command="{Binding ViewModel.NavigateCommand}"
                               CommandParameter="Network">
                    <DockPanel Margin="4,2">
                        <ui:SymbolIcon Symbol="Globe24" DockPanel.Dock="Left"
                                      FontSize="16" Margin="0,0,6,0"
                                      Foreground="#8B5CF6" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Network" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="Diagnostics" FontSize="9.5" Opacity="0.4" />
                        </StackPanel>
                    </DockPanel>
                </ui:CardAction>

                <ui:CardAction Grid.Column="2" Margin="4,0,4,0"
                               Command="{Binding ViewModel.NavigateCommand}"
                               CommandParameter="Registry">
                    <DockPanel Margin="4,2">
                        <ui:SymbolIcon Symbol="Key24" DockPanel.Dock="Left"
                                      FontSize="16" Margin="0,0,6,0"
                                      Foreground="#10B981" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Registry" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="Clean entries" FontSize="9.5" Opacity="0.4" />
                        </StackPanel>
                    </DockPanel>
                </ui:CardAction>

                <ui:CardAction Grid.Column="3" Margin="4,0,0,0"
                               Command="{Binding ViewModel.NavigateCommand}"
                               CommandParameter="Admin">
                    <DockPanel Margin="4,2">
                        <ui:SymbolIcon Symbol="ShieldTask24" DockPanel.Dock="Left"
                                      FontSize="16" Margin="0,0,6,0"
                                      Foreground="#F59E0B" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Admin" FontSize="11" FontWeight="SemiBold" />
                            <TextBlock Text="Services" FontSize="9.5" Opacity="0.4" />
                        </StackPanel>
                    </DockPanel>
                </ui:CardAction>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</Page>
