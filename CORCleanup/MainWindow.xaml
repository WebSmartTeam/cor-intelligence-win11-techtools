<ui:FluentWindow x:Class="CORCleanup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:CORCleanup.Views"
        Title="COR Cleanup"
        Width="1200"
        Height="620"
        MinWidth="900"
        MinHeight="550"
        WindowStartupLocation="CenterScreen"
        WindowCornerPreference="Round"
        WindowBackdropType="None"
        ExtendsContentIntoTitleBar="True"
        Icon="pack://application:,,,/Assets/cor-icon.png">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Custom title bar — grey tone, integrated with window chrome -->
        <ui:TitleBar x:Name="TitleBar"
                     Grid.Row="0"
                     Title="COR Cleanup"
                     Icon="pack://application:,,,/Assets/cor-icon.png" />

        <!-- Navigation sidebar + routed content -->
        <ui:NavigationView x:Name="NavigationView"
                           Grid.Row="1"
                           PaneDisplayMode="Left"
                           IsBackButtonVisible="Collapsed"
                           IsPaneToggleVisible="True"
                           OpenPaneLength="200">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="Auto Tool"
                                       TargetPageType="{x:Type views:AutoToolPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Wand24" Foreground="{StaticResource CorSectionAutoToolBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="Dashboard"
                                       TargetPageType="{x:Type views:HomePage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Home24" Foreground="{StaticResource CorSectionDashboardBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="Network"
                                       TargetPageType="{x:Type views:NetworkPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Globe24" Foreground="{StaticResource CorSectionNetworkBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="Cleanup"
                                       TargetPageType="{x:Type views:CleanupPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Broom24" Foreground="{StaticResource CorSectionCleanupBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="Uninstaller"
                                       TargetPageType="{x:Type views:UninstallerPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="AppsList24" Foreground="{StaticResource CorSectionUninstallerBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="System Info"
                                       TargetPageType="{x:Type views:HardwarePage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Desktop24" Foreground="{StaticResource CorSectionSystemInfoBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="Disk Tools"
                                       TargetPageType="{x:Type views:ToolsPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Wrench24" Foreground="{StaticResource CorSectionDiskToolsBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="Admin"
                                       TargetPageType="{x:Type views:AdminPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="ShieldTask24" Foreground="{StaticResource CorSectionAdminBrush}" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>

            <!-- Brand mark — full-width logo above footer items in the sidebar -->
            <ui:NavigationView.PaneFooter>
                <Border Padding="8,10" BorderThickness="0,1,0,0"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}">
                    <Image Source="pack://application:,,,/Assets/cor-intelligence-logo.png"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           HorizontalAlignment="Stretch"
                           Stretch="Uniform"
                           MaxHeight="40" />
                </Border>
            </ui:NavigationView.PaneFooter>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="Help"
                                       Icon="{ui:SymbolIcon BookQuestionMark24}"
                                       TargetPageType="{x:Type views:HelpPage}" />
                <ui:NavigationViewItem x:Name="IdeaPortalItem"
                                       Content="Idea Portal"
                                       Icon="{ui:SymbolIcon Lightbulb24}"
                                       ToolTip="Submit feature ideas on our website" />
                <ui:NavigationViewItem Content="Settings"
                                       Icon="{ui:SymbolIcon Settings24}"
                                       TargetPageType="{x:Type views:SettingsPage}" />
            </ui:NavigationView.FooterMenuItems>

        </ui:NavigationView>

        <!-- Status Bar -->
        <Border Grid.Row="2"
                Padding="12,4"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="7" Height="7" Fill="#4CAF50" Margin="0,0,6,0" />
                    <TextBlock Text="Ready"
                               FontSize="10.5"
                               Opacity="0.6"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <TextBlock DockPanel.Dock="Right"
                           Text="v1.0.18"
                           FontSize="10.5"
                           Opacity="0.4"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </DockPanel>
        </Border>
    </Grid>

</ui:FluentWindow>
