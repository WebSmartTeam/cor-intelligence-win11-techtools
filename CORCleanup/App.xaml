<Application x:Class="CORCleanup.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:CORCleanup.Converters"
             ShutdownMode="OnMainWindowClose"
             DispatcherUnhandledException="OnDispatcherUnhandledException">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- Global converters (shared across all pages) -->
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            <converters:StringToVisibilityConverter x:Key="StringToVis" />
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
            <converters:IntEqualsToVisibilityConverter x:Key="IntEqualsToVis" />
            <converters:NullToVisibilityConverter x:Key="NullToVis" />
            <converters:PercentToHeightConverter x:Key="PercentToHeight" />
            <converters:SignalToBrushConverter x:Key="SignalToBrush" />
            <converters:IntGreaterThanZeroToVisibilityConverter x:Key="IntGreaterThanZeroToVis" />

            <!-- Compact DataGrid — tight rows, no gridlines -->
            <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                <Setter Property="AutoGenerateColumns" Value="False" />
                <Setter Property="HeadersVisibility" Value="Column" />
                <Setter Property="GridLinesVisibility" Value="None" />
                <Setter Property="CanUserSortColumns" Value="True" />
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                <Setter Property="RowHeight" Value="30" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <!-- Compact DataGridRow -->
            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                <Setter Property="MinHeight" Value="26" />
            </Style>

            <!-- Compact TabItem — less padding -->
            <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                <Setter Property="Padding" Value="10,5" />
                <Setter Property="FontSize" Value="12" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
